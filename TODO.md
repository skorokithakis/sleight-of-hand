No pending items.

## Done

* ~~Add a `calibrate` mode where I can send an MQTT message of `calibrate <position>` and
  the clock will sprint from that position onward. For example, if I send `calibrate
  45`, that means "we're at p45" and the clock should sprint to p00 and start_at_minute.~~
